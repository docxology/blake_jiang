# `output/` — Disposable Build Artifacts

Working outputs of the pipeline and scripts (`manuscript_injection.json`,
`figures/`, reports). Gitignored except final deliverables; regenerate via the
canonical pipeline rather than editing by hand.

Currently contains only `.checkpoints/`.
